import { requireAdminAction } from "@/app/admin-auth";
import { addLiveMessage,getAdminConversation,getAdminConversations,getLiveMessages,updateLiveConversation } from "@/db/live-chat";
const clean=(v:unknown,max=500)=>String(v||"").replace(/[\u0000-\u001f\u007f]/g," ").replace(/\s+/g," ").trim().slice(0,max);
export async function GET(req:Request){try{await requireAdminAction();const id=clean(new URL(req.url).searchParams.get("conversationId"),50);if(!id)return Response.json({conversations:await getAdminConversations()});const chat=await getAdminConversation(id);if(!chat)return Response.json({message:"Không tìm thấy hội thoại."},{status:404});return Response.json({conversation:{...chat,token:undefined},messages:await getLiveMessages(id)},{headers:{"Cache-Control":"no-store"}});}catch{return Response.json({message:"Không có quyền truy cập."},{status:401});}}
export async function POST(req:Request){try{const admin=await requireAdminAction(),b=await req.json() as Record<string,unknown>,id=clean(b.conversationId,50),action=clean(b.action,20);const chat=await getAdminConversation(id);if(!chat)return Response.json({message:"Không tìm thấy hội thoại."},{status:404});if(action==="status"){const status=clean(b.status,20);if(!["waiting","active","closed"].includes(status))return Response.json({message:"Trạng thái không hợp lệ."},{status:400});await updateLiveConversation(id,status,admin.name);return Response.json({ok:true});}const text=clean(b.text);if(!text)return Response.json({message:"Tin nhắn trống."},{status:400});await updateLiveConversation(id,"active",admin.name);await addLiveMessage(id,"admin",admin.name,text);return Response.json({ok:true});}catch{return Response.json({message:"Không có quyền truy cập."},{status:401});}}
